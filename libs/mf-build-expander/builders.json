{
  "builders": {
    "browser": {
      "implementation": "./src/browser",
      "schema": "./src/browser/schema.json",
      "description": "Extends and builds the build target from a local project"
    },
    "dev-server": {
      "implementation": "./src/dev-server",
      "schema": "./src/dev-server/schema.json",
      "description": "Extends and serves the serve target from a local project"
    },
    "karma": {
      "implementation": "./src/karma",
      "schema": "./src/karma/schema.json",
      "description": "Extends and runs the test target from a local project"
    }
  }
}
