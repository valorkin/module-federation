//import { BuilderContext, BuilderOutput, createBuilder } from '@angular-devkit/architect';
//import { execute } from '@angular-devkit/build-angular/src/extract-i18n';
//import { Transforms } from 'ngx-build-plus/src/utils';
//import { expandWorkspaceOptions, overrideBuilderContext } from '../utils';

/**
 * Builder implementation
 */
/*export async function extractI18nWebpack(options: any, context: BuilderContext, transforms: Transforms = {}): Promise<BuilderOutput> {
  const expandedOptions = await expandWorkspaceOptions(options, context)
    .catch(error => console.log(error));

  const overridedContext = overrideBuilderContext(expandedOptions.browserTarget, context);
  return execute(expandedOptions, overridedContext, transforms);
}

export default createBuilder(extractI18nWebpack);*/

